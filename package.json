{
  "name": "esy-tree-sitter",
  "version": "1.1.0",
  "description": "esy-enabled build for tree-sitter",
  "license": "MIT",
  "esy": {
    "build": [
        ["bash", "-c", "#{os == 'windows' ? 'CC=x86_64-w64-mingw32-gcc' : os == 'linux' ? 'CFLAGS='-fPIC' : 'CC=gcc'} script/build-lib"]
    ],
    "install": [
        ["cp", "libtree-sitter.a", "#{self.lib}"],
        ["mkdir", "-p", "#{self.install / 'include'}"],
        ["cp", "-r",  "lib/include", "#{self.install}"]
    ],
    "buildsInSource": true,
    "exportedEnv": {
        "TREESITTER_INCLUDE_PATH": {
            "val": "#{self.install / 'include'}",
            "scope": "global"
        },
        "TREESITTER_LIB_PATH": {
            "val": "#{self.lib}",
            "scope": "global"
        }
    }
  },
  "dependencies": { }
}
